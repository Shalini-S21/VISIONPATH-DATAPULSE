import React from 'react';
import ComingSoon from '../shared/ComingSoon';

const NotificationCenter = () => (
  <ComingSoon title="Notification Center" description="Coordinate campaigns, alerts, and system notifications at scale." badge="Admin tools" />
);

export default NotificationCenter;
